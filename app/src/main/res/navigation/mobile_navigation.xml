<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_photo">

    <fragment
        android:id="@+id/nav_photo"
        android:name="com.dip.safewriter.fragment.PhotosFragment"
        android:label="@string/menu_photo"
        tools:layout="@layout/fragment_photos" />

    <fragment
        android:id="@+id/nav_collections"
        android:name="com.dip.safewriter.fragment.CollectionsFragment"
        android:label="@string/menu_collections"
        tools:layout="@layout/fragment_collections" />

    <fragment
        android:id="@+id/nav_favorite"
        android:name="com.dip.safewriter.fragment.FavoriteFragment"
        android:label="@string/menu_favorite"
        tools:layout="@layout/fragment_favorite" />
</navigation>